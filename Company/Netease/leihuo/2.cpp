#include<bits/stdc++.h>
using namespace std;
int main() {
	int n, m;
	cin >> n >> m;
	vector<vector<int>> tri;
	for(int i = 0;i < m ;i++) {
		vector<int> tri(3);
		cin >> tri[0] >> tri[1] >> tri[2];
		tris.push_back(tri);
	}

	//看第一个三角形是什么顺序？
	for(int i = 0;i < m;i++) {
		
	}
}